<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Also declared in app.json's android.permissions; repeated here so this
       module is self-describing, the same way expo-location's own manifest
       redeclares the location permissions app.json already grants. -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

  <application>
    <receiver
      android:name=".PrayerWidgetProvider"
      android:exported="true">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="expo.modules.prayerwidget.ACTION_TICK" />
      </intent-filter>
      <meta-data
        android:name="android.appwidget.provider"
        android:resource="@xml/widget_prayer_info" />
    </receiver>
  </application>
</manifest>
